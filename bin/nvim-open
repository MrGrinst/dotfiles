#!/usr/bin/env bash
# Thin launcher so macOS/AppleScript can call a stable path; logic lives in nvim-open.ts.
set -euo pipefail
export PATH="/opt/homebrew/bin:$HOME/.bun/bin:/usr/local/bin:$PATH"
exec bun run "$(cd "$(dirname "$0")" && pwd)/nvim-open.ts" "$@"
